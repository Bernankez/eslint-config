{
  "name": "@bernankez/eslint-config-monorepo",
  "version": "0.0.5",
  "private": true,
  "packageManager": "pnpm@7.1.0",
  "author": "Cole <bernankez@qq.com>",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "pnpm -r run test",
    "prepare": "pnpm -r run stub",
    "release": "nrm use npm && bumpp package.json packages/*/package.json && pnpm -r publish --access public && nrm use taobao"
  },
  "devDependencies": {
    "@bernankez/eslint-config": "*",
    "bumpp": "^8.2.1",
    "eslint": "^8.23.0",
    "typescript": "^4.8.2"
  }
}
